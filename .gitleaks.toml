[extend]
# Use the default gitleaks ruleset as the base
useDefault = true

# Add a [[allowlists]] block with at least one of: commits / paths / regexes /
# stopwords if a real false-positive is found. Empty allowlists are rejected
# by gitleaks 8.x.
